import 'package:flutter/material.dart';

//SCROLL CARDS constants
const dateTimeFontStyle = TextStyle(
  fontFamily: 'Quicksand',
  fontSize: 14,
  color: Colors.white,
  fontWeight: FontWeight.w500,
  height: 1.43, // line-height: 20px / font-size: 14px
);

const messageContentFontStyle = TextStyle(
  fontFamily: 'Quicksand',
  fontSize: 16,
  color: Colors.white,
  fontWeight: FontWeight.w500,
  height: 1.5, // line-height: 24px / font-size: 16px
);

const nameTextStyle = TextStyle(
  fontFamily: 'Quicksand',
  fontSize: 20,
  fontWeight: FontWeight.w600,
  color: Colors.white,
  height: 1.4,
);

const cardBackgroundColor = Color.fromRGBO(255, 255, 255, 0.02);

//HEADER BUTTONS constants
const headerPadding = EdgeInsets.all(16.0);
const headerIconColor = Color.fromRGBO(102, 109, 128, 1);
const headerTitleStyle = TextStyle(color: Colors.white, fontSize: 20);

//FOOTER BUTTONS constants
const footerPadding = EdgeInsets.all(16.0);
const footerButtonText = 'Günün Şanslı Sayısı';
const footerButtonBackgroundColor = Colors.white;
const footerButtonForegroundColor = Colors.black;
const footerButtonBorderRadius = 30.0;
const footerButtonSpacing = 12.0; // Updated gap
const footerEditIconColor = Colors.black;
const footerButtonHeight = 48.0; // Fixed height

//HOME constants
const backgroundGradientColors = [
  Colors.white,
  Colors.white,
  Color.fromRGBO(0, 0, 0, 0.6),
  Color.fromRGBO(0, 0, 0, 0.6),
];
